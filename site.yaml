---
- hosts: jenkins
  become: yes
  gather_facts: no
  roles:
    - role: common

- hosts: jenkins
  become: yes
  gather_facts: yes
  roles:
    - role: jenkins


      #- hosts: json-filter
      # become: yes
      #gather_facts: no
      #roles:
      #- role: common

      #- hosts: rabbit-to-bd
      #become: yes
      #gather_facts: no
      #roles:
      #- role: common

      #- hosts: rest-api
      #become: yes
      #gather_facts: no
      #roles:
      #- role: common
