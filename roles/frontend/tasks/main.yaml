---
- import_tasks: check_vars.yaml
  tags:
    - never
    - frontend_service_install
    - frontend_check_vars

- import_tasks: frontend.yaml
  tags:
    - never
    - frontend_service_install
    - frontend_install
