---
- name: Check that String Variables are set correctly 
  assert:
    that:
      - "{{ item is defined }}"
      - "{{ item is string }}"
      - "{{ item | length > 0 }}"
    quiet: yes
  loop:
    - "{{ common_ntp_timezone }}"
    - "{{ common_ntp_driftfile }}"
    - "{{ common_required_package | join(',') }}"
    - "{{ common_ntp_restrict | join(',') }}"
    - "{{ common_ntp_servers | join(',') }}"

- name: Check that Integer Variables are set correctly 
  assert:
    that:
      - "{{ item is defined }}"
      - "{{ item is number }}"
    quiet: yes
  loop:
    - "{{ common_protection }}"
